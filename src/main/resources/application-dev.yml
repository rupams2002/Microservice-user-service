server:
  port: 9002

spring:
  application:
    name: USER-SERVICE

#ADDED TO BOOTSTRAP FILE
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://192.168.1.188:8761/eureka/
  instance:
    hostname: localhost